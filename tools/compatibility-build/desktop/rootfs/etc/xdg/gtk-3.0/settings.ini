[Settings]
# Under full emulation a frame takes longer than the frame interval, so GTK
# animations (such as the Minecraft Launcher updater's spinner) redraw without
# pause and starve the program's lower-priority work: the updater never went
# on to start the launcher.
gtk-enable-animations=false
