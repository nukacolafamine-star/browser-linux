diff --git a/hw/core/machine.c b/hw/core/machine.c
index 0c17398..4c98967 100644
--- a/hw/core/machine.c
+++ b/hw/core/machine.c
@@ -1401,11 +1401,22 @@ void machine_run_board_init(MachineState *machine, const char *mem_path, Error *
     replay_checkpoint(CHECKPOINT_INIT);
 
     if (!xen_enabled()) {
+#ifdef __EMSCRIPTEN__
+        /*
+         * wasm32 linear memory is addressed as unsigned and grows to 4 GiB;
+         * leave room for the translation cache and QEMU itself.
+         */
+        if (machine->ram_size > (3072ULL << 20)) {
+            error_setg(errp, "at most 3072 MB RAM can be simulated");
+            return;
+        }
+#else
         /* On 32-bit hosts, QEMU is limited by virtual address space */
         if (machine->ram_size > (2047 << 20) && HOST_LONG_BITS == 32) {
             error_setg(errp, "at most 2047 MB RAM can be simulated");
             return;
         }
+#endif
     }
 
     if (machine->memdev) {
diff --git a/util/mmap-alloc.c b/util/mmap-alloc.c
index 8c8708c..f0688a8 100644
--- a/util/mmap-alloc.c
+++ b/util/mmap-alloc.c
@@ -252,6 +252,19 @@ void *qemu_ram_mmap(int fd,
 {
 #if defined(EMSCRIPTEN)
     void *ptr;
+    if (fd == -1) {
+        /*
+         * musl's mmap() refuses lengths of 2 GiB or more on 32-bit targets,
+         * and Emscripten serves anonymous mappings from the heap anyway.
+         */
+        ptr = qemu_try_memalign(align, size);
+        if (!ptr) {
+            errno = ENOMEM;
+            return MAP_FAILED;
+        }
+        memset(ptr, 0, size);
+        return ptr;
+    }
     ptr = mmap_activate(0, size + align, fd, qemu_map_flags, map_offset);
     return (void *)QEMU_ALIGN_UP((uintptr_t)ptr, align);
 #else
@@ -302,6 +315,12 @@ void *qemu_ram_mmap(int fd,
 
 void qemu_ram_munmap(int fd, void *ptr, size_t size)
 {
+#if defined(EMSCRIPTEN)
+    if (ptr && fd == -1) {
+        qemu_vfree(ptr);
+        return;
+    }
+#endif
     if (ptr) {
         /* Unmap both the RAM block and the guard page */
         munmap(ptr, size + mmap_guard_pagesize(fd));
